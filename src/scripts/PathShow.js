import DigiPath from './DigiPath';

export default function PathShow(initial, final, exceptions) {
  //const path = new DigiPath(initial, final, exceptions);
  console.log('path');
  return 'quaque';
}
